---
trigger: model_decision
description: After finishing the editing and before prompting the user (ending the conversation).
---

After editing is complete, please confirm whether the requirements are met and check if any code unrelated to this change has been mistakenly edited.
If any errors are discovered, please use the git command immediately to revert to the previous version.
編輯完成後,請確認使否符合使用者要求,並檢查是否有誤編輯到不涉及此次變更的代碼.
若有發現任何錯誤,請立即使用git命令回退到上一個版本.